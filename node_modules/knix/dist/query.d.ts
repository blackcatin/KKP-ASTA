import { SQLiteDatabase, Transaction } from "react-native-sqlite-storage";
export interface QueryParams {
    sqlite: SQLiteDatabase;
    tx?: Transaction;
    sql: string;
    params: any[];
}
export declare class Query<T> {
    private sqlite;
    private tx?;
    private sql;
    private params;
    constructor(data: QueryParams);
    private executeNewTransaction;
    private executeExistingTransaction;
    private execute;
    insert(): Promise<number>;
    update(): Promise<number>;
    one(): Promise<T | undefined>;
    all(): Promise<T[]>;
    exec(): Promise<void>;
}
//# sourceMappingURL=query.d.ts.map