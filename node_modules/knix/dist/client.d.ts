import { SQLiteDatabase, Transaction } from "react-native-sqlite-storage";
import { Query } from "./query";
export interface IClient {
    query<T>(sql: string, params?: any[]): Query<T>;
}
export declare class TransactedClient implements IClient {
    private sqlite;
    private tx;
    constructor(sqlite: SQLiteDatabase, tx: Transaction);
    query<T>(sql: string, params?: any[]): Query<T>;
}
export declare class Client implements IClient {
    private sqlite;
    constructor(sqlite: SQLiteDatabase);
    query<T>(sql: string, params?: any[]): Query<T>;
    transaction(cb: (client: TransactedClient) => Promise<unknown>): Promise<void>;
}
//# sourceMappingURL=client.d.ts.map